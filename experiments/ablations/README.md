# Дополнительные абляции

В эту группу входят значения `beta=0.3, tau=0.1` из исходной статьи, четыре off-topic негатива, градиент через негативную ветвь и REINFORCE со сэмплированием ответа.

```bash
python experiments/ablations/run.py
```

Эти эксперименты дополняют основные таблицы и подробно описаны в `docs/EXPERIMENT_LOG.md`.

